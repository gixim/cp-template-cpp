#include <iostream>
using namespace std;

void solve(void) {
}

int main(void) {
    cin.tie(nullptr);
    ios::sync_with_stdio(false);

    int t = 1;
    // cin >> t;
    while (t--) {
        solve();
    }

    return 0;
}
